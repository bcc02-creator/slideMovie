{
  "name": "slidecast",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "投影片 + 配音 + 字幕 → 影片",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "pdfjs-dist": "3.11.174",
    "@ffmpeg/ffmpeg": "^0.11.6",
    "@ffmpeg/core": "^0.11.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10",
    "gh-pages": "^6.2.0"
  }
}
